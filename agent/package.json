{
  "name": "nadia-agent",
  "version": "0.0.1",
  "private": true,
  "description": "Nadia AI agent, built on the terraforming-mars Engine in this repo.",
  "scripts": {
    "create-game": "tsx src/runner/createGameCli.ts",
    "bench": "tsx src/runner/speedSpikeCli.ts",
    "determinism": "tsx src/runner/determinismCli.ts",
    "legality": "tsx src/runner/legalityCli.ts",
    "coverage": "tsx src/runner/coverageCli.ts",
    "match": "tsx src/runner/matchCli.ts",
    "match:pool": "tsx src/match/pool.ts",
    "determinism:sweep": "tsx src/determinism/sweep.ts",
    "determinism:cross-process": "tsx src/determinism/childReplay.ts",
    "test": "mocha --import=tsx --require ../tests/testing/setup.ts \"test/**/*.spec.ts\""
  }
}
